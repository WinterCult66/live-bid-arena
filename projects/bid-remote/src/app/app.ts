import { Component } from '@angular/core';

@Component({
  selector: 'app-bid-remote',
  templateUrl: './app.html',
  styleUrl: './app.css',
})
export class App {
  readonly presets = [10, 50, 100];
}
